# funkce porovna proměno hodnotu s 3 a zjistí, zda je menší, větší nebo rovna
def cislo_mensi_nez_3(hodnota):

    if hodnota > 3:
        print(f"Hodnota {hodnota} je vetsi nez 3")
    elif hodnota < 3:
        print(f"Hodnota {hodnota} je mensi nez 3")
    else:
        print(f"Hodnota {hodnota} je rovna 3")
       

# f = formátovaný; řekne programu, ať nevypisuje {hodnota}, ale tu hodnotu tam skutečně vloží


if __name__ == "__main__":    
    cislo = input("Zadej číslo: ")
    cislo = int(cislo)
    print(f"Zadané číslo je: {cislo}")

    cislo_mensi_nez_3(1)
    cislo_mensi_nez_3(cislo)
